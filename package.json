{
  "name": "zabloo-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build:example": "node --import @zabloo/cli/register packages/cli/src/bin.ts build examples/button/app.tsx"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
