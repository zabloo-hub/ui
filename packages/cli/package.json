{
  "name": "@zabloo/cli",
  "version": "0.1.0",
  "description": "zabloo/ui CLI: export React projects as versioned IR envelopes",
  "license": "MIT",
  "type": "module",
  "bin": {
    "zabloo": "./dist/cli.js",
    "zb": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean --sourcemap",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@zabloo/format": "workspace:*",
    "commander": "^15.0.0",
    "jiti": "^2.7.0"
  }
}
