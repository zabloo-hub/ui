/**
 * The typed editors of the bindings panel (V15). V14 imports the field from
 * here and nothing else: which control a type gets is this folder's business.
 */
export { BindingField, type BindingFieldProps, UI_MARK_MS } from "./BindingField";
