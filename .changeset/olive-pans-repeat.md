---
"create-zabloo-app": patch
---

Scaffolded projects get a `dev:godot` script (`zabloo dev --godot`), and the README says how
to run the Godot dev loop: enable the addon, press Play, and every save hot-swaps the
running view.
