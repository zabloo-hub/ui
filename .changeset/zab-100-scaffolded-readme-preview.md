---
"create-zabloo-app": patch
---

The README scaffolded into a new project describes the current dev preview — the typed
bindings panel and the console — instead of the old data panel and view picker.
