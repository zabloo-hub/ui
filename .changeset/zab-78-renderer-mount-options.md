---
"@zabloo/renderer-web": minor
---

pr: 58

Two new `MountOptions`: `dpr` overrides the device pixel ratio the renderer rasterizes at, and
`onFrame` fires after every painted frame with its `FrameStats`.
