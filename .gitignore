# --- Sistema operativo ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editores / IDE ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Variables de entorno y secretos ---
.env
.env.*
!.env.example
*.local

# --- Dependencias (Node, por si acaso) ---
node_modules/
.pnp
.pnp.js

# --- Salidas de build comunes ---
dist/
*.tsbuildinfo
build/
out/
.cache/
coverage/

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Misc ---
.tmp/
tmp/

# --- WASM intermedio (el base64 committeado en src/generated es la fuente) ---
packages/renderer-web/native/*.wasm

# --- Build nativo (core C++ + GDExtension de Godot) ---
# Los binarios se compilan; el addon publicable se arma en release (2026-08-24).
core/bin/
sdk/godot/bin/
sdk/godot/addons/zabloo/bin/
# El plugin nativo del paquete de Unity lo copia `scons install` (sdk/unity) desde
# core/bin, con su .meta generado al lado (2026-09-03, UN3).
sdk/unity/Runtime/Plugins/**/libzabloo.*
sdk/unity/Runtime/Plugins/**/zabloo.dll*
.sconsign.dblite
*.o
*.obj
*.os
*.a
*.lib
*.pdb
*.ilk
*.exp
*.dSYM/

# --- Contexto de Claude ---
# CLAUDE.md y docs/internal/ SÍ se versionan: el repo se explica solo (2026-08-23).
# Solo lo local del entorno queda fuera:
.claude/settings.local.json
