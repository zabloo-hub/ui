namespace Zabloo.Editor
{
    /// <summary>
    /// Editor-side tooling for the zabloo SDK (envelope import; later, the dev mode
    /// that receives hot-pushes from `zabloo dev`). Skeleton stub.
    /// </summary>
    internal static class ZablooEditor
    {
    }
}
