[plugin]

name="Zabloo UI"
description="Renders zabloo/ui content inside Godot: the engine-agnostic IR is tessellated into GPU geometry by the shared core, so the same envelope draws the same pixels on every target."
author="zabloo"
version="0.2.0"
script="zabloo_plugin.gd"
